// eslint-disable-next-line no-unused-vars
({ userId }) => {
  //   const quote = domain.marketData.quotes.getQuoteSigner({ userId });
  //   if (quote) {
  //     // if (quote.value.signers.size === 1) {
  //     //   console.warn('quote.value.signers ===1: ', quote);
  //     //   domain.marketData.tvClient.client.deleteQuoteSymbol({ symbol: quote.symbol });
  //     // }
  //     quote.value.signers.delete(userId);
  //   }
};
