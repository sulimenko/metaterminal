services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    environment:
      NODE_ENV: production
    volumes:
      - ./log:/usr/server/log
    networks:
      ptfin-backend:
        aliases:
          - metaterminal.ptfin.local

networks:
  ptfin-backend:
    external: true
    name: ptfin-backend
